Привет! Ты будешь исправлять проект Chaos Addon (Minecraft-мод на NeoForge 1.21.1) по результатам проверки, которую провела другая ИИ. У тебя нет исходного ТЗ, но есть подробный отчёт с перечнем недочётов. Ниже я перечисляю конкретные задачи, которые нужно выполнить.

1. Добавить отсутствующую ультимативную способность bloom для расы Mycelial Symbiont
Создай файл src/main/resources/data/chaos_addon/powers/mycelial_symbiont/bloom.json.

Механика (по ТЗ): взрывной рост мха в радиусе 10–15 блоков, союзники получают лечение, враги – замедление и слабость, временно создаётся "суперузел" с увеличенным радиусом баффа.

Используй тип neoorigins:active_ability, кулдаун 6000 тиков (5 минут), клавиша key.tertiary (третичная, по умолчанию 'X').

Пропиши "command": "chaos_addon_bloom".

Добавь ID "chaos_addon:mycelial_symbiont/bloom" в массив "powers" в файле origins/mycelial_symbiont.json.

Зарегистрируй команду chaos_addon_bloom в init/ModCommands.java – создай для неё класс-обработчик (например, BloomCommand) и добавь его в список регистрации.

Реализуй логику в MycelialSymbiontHandler.java (или в новом классе) – при активации создай область с эффектами, спавн частиц мха, лечение игроков, дебаффы на мобов.

2. Удалить мёртвый код-дубликат
Удали файл DeadLegionCommand.java (находится в src/main/java/.../commands/ или аналогичной папке). Он никогда не регистрируется и дублирует функциональность NecrovoreCommands.java. Просто удали файл целиком.

3. Исправить хардкод в BloodSmithHandler
В файле BloodSmithHandler.java найди метод, отвечающий за способность survival_instinct (или любое место, где используется кулдаун 600).
Замени 600 на ChaosAddonConfig.get().bloodSacrificeCooldown.
Убедись, что поле bloodSacrificeCooldown уже существует в ChaosAddonConfig.java – согласно отчёту оно есть со значением 600. Если его нет – создай.

4. Провести полный аудит всех хендлеров на магические числа
В отчёте сказано, что только 21 из 57 файлов используют конфиг. Твоя задача – найти все оставшиеся хардкодные числа (интервалы, радиусы, проценты, уроны, кулдауны, длительности) во всех .java файлах внутри src/main/java/, кроме очевидных 0, 1, 100% и стандартных констант типа Math.PI.

Для каждой найденной константы, которая должна быть настраиваемой, создай поле в ChaosAddonConfig.java с осмысленным именем (например, mountainEchoInterval, falseLootDamage, parasiteSenseRange и т.д.) и значением по умолчанию, взятым из существующего кода.

Замени все такие литералы на обращения к конфигу (например, ChaosAddonConfig.get().someField).

Сделай это для всех хендлеров, включая GeneralPowerHandler, RadioactiveHandler, ParasiteHandler, ArchaeologistHandler, BiomorphHandler, DeepGeomancerHandler, DimensionJudgeHandler, WanderingGardenerHandler, AlchemistHandler и любые другие.

5. Дополнительно (если останется время и желание)
Если dimension_judge/annihilate.json и equalize.json – orphan-файлы, которые не добавлены в origin и не используются, ты можешь их удалить (но это не обязательно, они безвредны).

Проверь, что все команды, на которые ссылаются execute_command в JSON-способностях, действительно зарегистрированы в ModCommands.java.

После всех правок выполни чистую сборку командой ./gradlew build (если у тебя есть доступ к Maven). Если сборка успешна – отлично, если нет – исправь ошибки компиляции.

6. Итоговый отчёт
После завершения всех задач предоставь мне отчёт, в котором будет:

Список созданных/изменённых файлов.

Краткое описание внесённых изменений (особенно для bloom.json и вынесенных в конфиг параметров).

Результат сборки (если делал).

Любые замечания или обнаруженные проблемы.

Важно: Ты работаешь в том же репозитории, что и предыдущая ИИ, поэтому все изменения должны быть согласованы с существующей структурой. Не переименовывай файлы без крайней необходимости, не меняй API NeoOrigins – просто добавляй недостающее и исправляй хардкоды.